openapi: 3.0.0
info:
  version: 1.0.0
  title: Title
paths:
  /foo:
    post:
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Body'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Body'
components:
  schemas:
    Body:
      type: object
      properties:
        a:
          $ref: 'a.yaml#/components/schemas/A'
        b1:
          $ref: 'b.yaml#/components/schemas/B1'
